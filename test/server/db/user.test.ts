import 'mocha';
import { expect } from 'chai';

describe('User', () => {
  it('can be created');
  it('needs an email to be created');
  it('can be updated');
  it('can be deleted');
  it('can be listed');
  it('can be queried');
  it('can navigate to first language');
  it('can navigate to learned languages');
});
